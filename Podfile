# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'efi-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for efi-ios
  pod 'iCarousel', '~> 1.8'

  target 'efi-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'efi-iosUITests' do
    # Pods for testing
  end

end
